#include <stdio.h>
#include <stdlib.h>

int main(){
    int n;
    printf("Donner moi un nombre:");
    scanf("%d",&n);
    if(n%2==0)h
        printf("le nombre%d est: impair",n);
    else
        printf("le nombre%d est: pair",n);
    return 0;
}
